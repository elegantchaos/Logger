# --------------------------------------------------------------------------------
# This workflow was automatically generated by Action Status 1.0 (213).
# (see https://actionstatus.elegantchaos.com for more details)
# --------------------------------------------------------------------------------

name: tests

on: [push, pull_request]

jobs:

    macOS-xcode-swift-50:
        name: macOS (Swift 5.0)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-macOS-xcode-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-macOS-xcode-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'macOS (Swift 5.0)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    macOS-xcode-swift-51:
        name: macOS (Swift 5.1)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-macOS-xcode-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-macOS-xcode-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'macOS (Swift 5.1)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    macOS-xcode-swift-52:
        name: macOS (Swift 5.2 Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="swift-5.2-branch"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-macOS-xcode-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-macOS-xcode-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'macOS (Swift 5.2 Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    macOS-xcode-swift-nightly:
        name: macOS (Swift Development Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="development"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-macOS-xcode-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-macOS-xcode-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-macOS-xcode.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME"  -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-macOS-xcode-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'macOS (Swift Development Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    iOS-swift-50:
        name: iOS (Swift 5.0)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-iOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-iOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'iOS (Swift 5.0)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    iOS-swift-51:
        name: iOS (Swift 5.1)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-iOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-iOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'iOS (Swift 5.1)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    iOS-swift-52:
        name: iOS (Swift 5.2 Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="swift-5.2-branch"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-iOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-iOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'iOS (Swift 5.2 Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    iOS-swift-nightly:
        name: iOS (Swift Development Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="development"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-iOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-iOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-iOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone 11" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-iOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'iOS (Swift Development Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    tvOS-swift-50:
        name: tvOS (Swift 5.0)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-tvOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-tvOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'tvOS (Swift 5.0)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    tvOS-swift-51:
        name: tvOS (Swift 5.1)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-tvOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-tvOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'tvOS (Swift 5.1)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    tvOS-swift-52:
        name: tvOS (Swift 5.2 Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="swift-5.2-branch"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-tvOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-tvOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'tvOS (Swift 5.2 Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    tvOS-swift-nightly:
        name: tvOS (Swift Development Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="development"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-tvOS-build-release.log | xcpretty
        - name: Test (Debug)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO  | tee logs/xcodebuild-tvOS-test-debug.log | xcpretty
        - name: Test (Release)
          run: |
            set -o pipefail
            source "names-tvOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-tvOS-test-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'tvOS (Swift Development Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    watchOS-swift-50:
        name: watchOS (Swift 5.0)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'watchOS (Swift 5.0)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    watchOS-swift-51:
        name: watchOS (Swift 5.1)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'watchOS (Swift 5.1)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    watchOS-swift-52:
        name: watchOS (Swift 5.2 Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="swift-5.2-branch"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'watchOS (Swift 5.2 Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    watchOS-swift-nightly:
        name: watchOS (Swift Development Nightly)
        runs-on: macOS-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Install Toolchain
          run: |
            branch="development"
            wget --quiet https://swift.org/builds/$branch/xcode/latest-build.yml
            grep "download:" < latest-build.yml > filtered.yml
            sed -e 's/-osx.pkg//g' filtered.yml > stripped.yml
            sed -e 's/:[^:\/\/]/YML="/g;s/$/"/g;s/ *=/=/g' stripped.yml > snapshot.sh
            source snapshot.sh
            echo "Installing Toolchain: $downloadYML"
            wget --quiet https://swift.org/builds/$branch/xcode/$downloadYML/$downloadYML-osx.pkg
            sudo installer -pkg $downloadYML-osx.pkg -target /
            ln -s "/Library/Developer/Toolchains/$downloadYML.xctoolchain/usr/bin" swift-latest
            export PATH="swift-latest:$PATH"
            swift --version
        - name: Xcode Version
          run: xcodebuild -version
        - name: XC Pretty
          run: sudo gem install xcpretty-travis-formatter
        - name: Detect Workspace & Scheme
          run: |
            WORKSPACE="Logger.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
            WORKSPACE="."
            GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Logger-Package || true))
            if [[ $GOTPACKAGE != "" ]]
            then
            SCHEME="Logger-Package"
            else
            SCHEME="Logger"
            fi
            MACOS_SCHEME="$SCHEME"
            IOS_SCHEME="$SCHEME"
            TVOS_SCHEME="$SCHEME"
            WATCHOS_SCHEME="$SCHEME"
            else
            MACOS_SCHEME="Logger-macOS"
            IOS_SCHEME="Logger-iOS"
            TVOS_SCHEME="Logger-tvOS"
            WATCHOS_SCHEME="Logger-watchOS"
            fi
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$MACOS_SCHEME'" > names-macOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$IOS_SCHEME'" > names-iOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$TVOS_SCHEME'" > names-tvOS.sh
            echo "WORKSPACE='$WORKSPACE'; SCHEME='$WATCHOS_SCHEME'" > names-watchOS.sh
        - name: Build (Debug)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Debug CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-debug.log | xcpretty
        - name: Build (Release)
          run: |
            set -o pipefail
            source "names-watchOS.sh"
            export PATH="swift-latest:$PATH"
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | tee logs/xcodebuild-watchOS-build-release.log | xcpretty
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'watchOS (Swift Development Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    linux-swift-50:
        name: Linux (Swift 5.0)
        runs-on: ubuntu-latest
        container: swift:5.0
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Build (Debug)
          run: swift build -c debug
        - name: Build (Release)
          run: swift build -c release
        - name: Test (Debug)
          run: swift test --configuration debug  
        - name: Test (Release)
          run: swift test --configuration release -Xswiftc -enable-testing 
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'Linux (Swift 5.0)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    linux-swift-51:
        name: Linux (Swift 5.1)
        runs-on: ubuntu-latest
        container: swift:5.1
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Build (Debug)
          run: swift build -c debug
        - name: Build (Release)
          run: swift build -c release
        - name: Test (Debug)
          run: swift test --configuration debug  --enable-test-discovery
        - name: Test (Release)
          run: swift test --configuration release -Xswiftc -enable-testing 
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'Linux (Swift 5.1)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    linux-swift-52:
        name: Linux (Swift 5.2 Nightly)
        runs-on: ubuntu-latest
        container: swiftlang/swift:nightly-5.2
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Build (Debug)
          run: swift build -c debug
        - name: Build (Release)
          run: swift build -c release
        - name: Test (Debug)
          run: swift test --configuration debug  --enable-test-discovery
        - name: Test (Release)
          run: swift test --configuration release -Xswiftc -enable-testing --enable-test-discovery
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'Linux (Swift 5.2 Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}


    linux-swift-nightly:
        name: Linux (Swift Development Nightly)
        runs-on: ubuntu-latest
        container: swiftlang/swift:nightly
        steps:
        - name: Checkout
          uses: actions/checkout@v1
        - name: Swift Version
          run: swift --version
        - name: Make Logs Directory
          run: mkdir logs
        - name: Build (Debug)
          run: swift build -c debug
        - name: Build (Release)
          run: swift build -c release
        - name: Test (Debug)
          run: swift test --configuration debug  --enable-test-discovery
        - name: Test (Release)
          run: swift test --configuration release -Xswiftc -enable-testing --enable-test-discovery
        - name: Upload Logs
          uses: actions/upload-artifact@v1
          with:
            name: logs
            path: logs
        - name: Slack Notification
          uses: elegantchaos/slatify@master
          if: always()
          with:
            type: ${{ job.status }}
            job_name: 'Linux (Swift Development Nightly)'
            mention_if: 'failure'
            url: ${{ secrets.SLACK_WEBHOOK }}

